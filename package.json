{
  "name": "dreamrealm",
  "private": true,
  "version": "0.1.0",
  "description": "DreamRealm — A production-grade dating ecosystem powered by DreamCoin.",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^1.13.4"
  },
  "dependencies": {
    "expo": "^55.0.24"
  }
}
